; Bump upload limits so product photos under 20 MB go through.
; Applies on FastCGI / PHP-FPM (the production deploy path).
upload_max_filesize = 20M
post_max_size       = 24M
memory_limit        = 256M
max_file_uploads    = 20
